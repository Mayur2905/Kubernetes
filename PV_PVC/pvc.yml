apiVersion: v1
kind: PersistentVolumeClaim
metdata:
  name: pvc-claim
spec:
  storageClassName: standard
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
