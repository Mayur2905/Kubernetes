apiVersion: apps/v1
kind: statefulsets

metadata:
  name: stateapp
  labels:
    app: statefull

spec:
  selector:
    matchLabels:
      app: statefull
  replicas: 5
  template:
    metadata:
      labels:
        app: mysqlapp

    spec:
      containers:
      - name: mysql
        image: mysql:latest
        port:
          containerPort: 3306
          protocol: TCP
